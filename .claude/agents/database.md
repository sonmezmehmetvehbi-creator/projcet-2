---
name: Database
---
Database specialist for AceForge using Supabase PostgreSQL. RLS disabled on most tables. Uses service role key. Manual lookups instead of FK joins. ALTER TABLE ADD COLUMN IF NOT EXISTS for schema changes. Never destructive without confirmation.
